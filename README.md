# 🤖 Многофункциональный Telegram Бот

Telegram-бот с AI-ассистентом, погодой, курсами валют, отслеживанием цен и напоминаниями.

## 🚀 Возможности

- 🤖 **AI-ассистент** (Mistral AI)
  - Обычный режим
  - Специализированный кино-режим
- 🎬 **Подбор фильмов** по жанрам
- 🌤 **Погода** в любом городе
- 💰 **Курсы валют** (USD, BTC)
- 📊 **Отслеживание цен** на Wildberries и Ozon
- ⏰ **Напоминания и таймеры**
- 🎤 **Транскрибация** голосовых сообщений

## 📦 Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/deadogdas/tg_bot.git
cd tg_bot
```

2. Создайте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. Создайте `.env` файл:

```bash
cp .env.example .env
```

5. Заполните `.env` своими ключами:

```env
BOT_TOKEN=your_bot_token
AI_KEY=your_mistral_key
WEATHER_KEY=your_weather_key
OPENAI_KEY=your_openai_key  # опционально
```

## 🎮 Использование

Запустите бота:

```bash
python main.py
```

### Команды

**Базовые:**

- `/start` — приветствие
- `/help` — список команд

**AI:**

- `/ai` — включить обычный ИИ
- `/movie_ai` — включить кино-ИИ
- `/ai_off` — выключить ИИ

**Фильмы:**

- `/movie` — подбор по жанру

**Финансы:**

- `/usd` — курс доллара
- `/btc` — курс Bitcoin

**Погода:**

- `/weather <город>` — погода

**Напоминания:**

- `/remind <время> <текст>` — напоминание
- `/timer <время> [текст]` — таймер
- `/reminders` — список

**Цены:**

- `/track <ссылка> [цена]` — отслеживать
- `/tracked` — список товаров

**Голос:**

- Отправьте голосовое → получите текст

## 🔑 Получение API ключей

- **Telegram Bot Token**: [@BotFather](https://t.me/BotFather)
- **Mistral AI**: [console.mistral.ai](https://console.mistral.ai)
- **OpenWeatherMap**: [openweathermap.org/api](https://openweathermap.org/api)
- **OpenAI** (опционально): [platform.openai.com](https://platform.openai.com)

## 📂 Структура проекта

```
tg_bot/
├── handlers/          # Обработчики команд
├── weather/           # Модуль погоды
├── states/            # FSM состояния
├── utils/             # Вспомогательные функции
├── data/              # Данные (создаётся автоматически)
├── config.py          # Конфигурация
├── main.py            # Точка входа
└── requirements.txt   # Зависимости
```

## 🐛 Логи

Логи сохраняются в `bot.log` с авторотацией (макс. 5 MB).
